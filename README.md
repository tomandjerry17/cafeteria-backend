# Cafeteria Ordering & Inventory Backend

Backend for USTP Cafeteria Ordering & Inventory System.  
Built with **Node.js, Express, TypeScript, Prisma, PostgreSQL (Supabase)**.

## Setup
1. Install dependencies:
   ```bash
   npm install
